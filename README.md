# Rentomo Web

Customer application serving booking and pricing pages using Go HTTP handlers.

Run `go run ./cmd/web`, then visit http://localhost:8080/booking.
Run `go test ./...` to check the application. All data is held in memory.
